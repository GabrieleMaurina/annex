{
  "private": true,
  "workspaces": ["server", "client", "mapper", "engine"],
  "scripts": {
    "check": "npm run check --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}

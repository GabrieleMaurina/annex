
# Commit

3. Review PROTOCOL.md. Make sure it is clear, precise, complete, and minimal. Ensure it has not contradictions. Remove repetitions and unnecessary prose.

4. Review all server .ts files. Make sure they are clear, precise, complete, minimal, free of bugs, and follow the coding style for the project. Additionally evaluate if a refactoring or reorganization is necessary. Improving organization or consistency might improvee future manintanance.

5. Review all client .ts and .tsx files. Make sure they are clear, precise, complete, minimal, free of bugs, and follow the coding style for the project. Additionally evaluate if a refactoring or reorganization is necessary. Improving organization or consistency might improvee future manintanance.


# Commit


6. GAME PROGRESSION

Server tracks the game phase with "lobby" or "playing". Refactor that and call it game state instead of phase. This refactoring should happend in client, server, and protocol.

Once the game has started, the game proceeds as follows: for each player in turn order, the player turn has 3 phases: deploy, attack, and fortify. Only a player whose turn it is can move on to the next phase. Once a player has completed all three phases, the turn moves to the next player. The server enforces the turn limit, so that if a player does not complete their turn in time, the turn will automatically move to the next player.

Update server and client model/protocol to include and track the following:
- turn counter: starts at 0 and goes up by one every time all remaining players have completed their turn
- current player in turn: integer index of the player in turn order
- current turn phase: one of "deploy", "attack", or "fortify"

At the bottom center of the game screen add a little semi transparent panel that shows the current phase for the current player in turn. If the local player is the current player in turn, also add a button to move to the next phase.

At the top of the screen add a think progress bar that shows the time spent in the current turn by the current player in turn. The progress bar is only a few pixels thick and spans the width of the screen. It has not borders and has the color of the player in turn. The progress bar should be updated smoothly by the client. The server does not send the state of the progress bar, but rather every client keeps track of the time spent in the current turn and updates the progress bar accordingly. The server only notifies when the turn has changed (and what a turn phase has changed to).


Do not implement anything inside the 3 turn phases. Just implement the protocol, tracking, UI, and time limit.


# Commit
